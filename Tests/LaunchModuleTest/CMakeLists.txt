# LaunchModuleTest: verifies GuardedMain lifecycle orchestration
# main -> GuardedMain -> FEngineLoop (PreInit->Init->Tick loop->Exit)
cmake_minimum_required(VERSION 3.28)
project(LaunchModuleTest LANGUAGES CXX)

set(CMAKE_CXX_STANDARD 23)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
set(CMAKE_CXX_EXTENSIONS OFF)

if(MSVC)
    add_compile_options(/std:c++latest /Zc:preprocessor)
elseif(CMAKE_CXX_COMPILER_ID MATCHES "GNU|Clang")
    add_compile_options(-std=c++2c)
endif()

set(CMAKE_RUNTIME_OUTPUT_DIRECTORY ${CMAKE_BINARY_DIR}/Bin)
set(CMAKE_LIBRARY_OUTPUT_DIRECTORY ${CMAKE_BINARY_DIR}/Bin)

set(ENGINE_ROOT "${CMAKE_CURRENT_SOURCE_DIR}/../../Engine/Source/Runtime")
set(CORE_PUBLIC "${ENGINE_ROOT}/Core/Public")
set(CORE_PRIVATE "${ENGINE_ROOT}/Core/Private")
set(ENGINE_PUBLIC "${ENGINE_ROOT}/Engine/Public")
set(ENGINE_PRIVATE "${ENGINE_ROOT}/Engine/Private")
set(LAUNCH_PUBLIC "${ENGINE_ROOT}/Launch/Public")
set(LAUNCH_PRIVATE "${ENGINE_ROOT}/Launch/Private")
set(APPCORE_PUBLIC "${ENGINE_ROOT}/ApplicationCore/Public")
set(APPCORE_PRIVATE "${ENGINE_ROOT}/ApplicationCore/Private")
set(RENDERCORE_PUBLIC "${ENGINE_ROOT}/RenderCore/Public")

# Generated API headers (normally produced by BuildTool)
set(GENERATED_DIR "${CMAKE_BINARY_DIR}/Generated")
file(MAKE_DIRECTORY "${GENERATED_DIR}/Core/Public")
file(MAKE_DIRECTORY "${GENERATED_DIR}/Engine/Public")
file(MAKE_DIRECTORY "${GENERATED_DIR}/Launch/Public")
file(MAKE_DIRECTORY "${GENERATED_DIR}/ApplicationCore/Public")
file(WRITE "${GENERATED_DIR}/Core/Public/CoreAPI.generated.h"
"// Auto-generated by CMake for standalone test builds.\n"
"#pragma once\n"
"\n"
"#include \"HAL/Platform.h\"\n"
"\n"
"#ifdef CORE_STATIC_DEFINE\n"
"    #define CORE_API\n"
"#elif defined(CORE_EXPORTS)\n"
"    #define CORE_API DLLEXPORT\n"
"#else\n"
"    #define CORE_API DLLIMPORT\n"
"#endif\n"
)
file(WRITE "${GENERATED_DIR}/Engine/Public/EngineAPI.generated.h"
"// Auto-generated by CMake for standalone test builds.\n"
"#pragma once\n"
"\n"
"#include \"HAL/Platform.h\"\n"
"\n"
"#ifdef ENGINE_STATIC_DEFINE\n"
"    #define ENGINE_API\n"
"#elif defined(ENGINE_EXPORTS)\n"
"    #define ENGINE_API DLLEXPORT\n"
"#else\n"
"    #define ENGINE_API DLLIMPORT\n"
"#endif\n"
)
file(WRITE "${GENERATED_DIR}/Launch/Public/LaunchAPI.generated.h"
"// Auto-generated by CMake for standalone test builds.\n"
"#pragma once\n"
"\n"
"#include \"HAL/Platform.h\"\n"
"\n"
"#ifdef LAUNCH_STATIC_DEFINE\n"
"    #define LAUNCH_API\n"
"#elif defined(LAUNCH_EXPORTS)\n"
"    #define LAUNCH_API DLLEXPORT\n"
"#else\n"
"    #define LAUNCH_API DLLIMPORT\n"
"#endif\n"
)
file(WRITE "${GENERATED_DIR}/ApplicationCore/Public/ApplicationCoreAPI.generated.h"
"// Auto-generated by CMake for standalone test builds.\n"
"#pragma once\n"
"\n"
"#include \"HAL/Platform.h\"\n"
"\n"
"#ifdef APPLICATIONCORE_STATIC_DEFINE\n"
"    #define APPLICATIONCORE_API\n"
"#elif defined(APPLICATIONCORE_EXPORTS)\n"
"    #define APPLICATIONCORE_API DLLEXPORT\n"
"#else\n"
"    #define APPLICATIONCORE_API DLLIMPORT\n"
"#endif\n"
)
file(MAKE_DIRECTORY "${GENERATED_DIR}/RenderCore/Public")
file(WRITE "${GENERATED_DIR}/RenderCore/Public/RenderCoreAPI.generated.h"
"// Auto-generated by CMake for standalone test builds.\n"
"#pragma once\n"
"\n"
"#include \"HAL/Platform.h\"\n"
"\n"
"#ifdef RENDERCORE_STATIC_DEFINE\n"
"    #define RENDERCORE_API\n"
"#elif defined(RENDERCORE_EXPORTS)\n"
"    #define RENDERCORE_API DLLEXPORT\n"
"#else\n"
"    #define RENDERCORE_API DLLIMPORT\n"
"#endif\n"
)

# -- Core (SHARED) --
add_library(Core SHARED
    "${CORE_PRIVATE}/Modules/ModuleInitializerEntry.cpp"
    "${CORE_PRIVATE}/Modules/ModuleManager.cpp"
    "${CORE_PRIVATE}/Logging/LogSystem.cpp"
    "${CORE_PRIVATE}/Misc/AssertionMacros.cpp"
    "${CORE_PRIVATE}/Misc/CoreGlobals.cpp"
    "${CORE_PRIVATE}/Misc/ConfigCacheIni.cpp"
    "${CORE_PRIVATE}/Misc/ConfigDelegates.cpp"
    "${CORE_PRIVATE}/CoreModule.cpp"
)
target_include_directories(Core PUBLIC "${CORE_PUBLIC}" "${GENERATED_DIR}/Core/Public")
target_compile_definitions(Core PRIVATE CORE_EXPORTS)

# -- ApplicationCore (SHARED) --
add_library(ApplicationCore SHARED
    "${APPCORE_PRIVATE}/ApplicationCoreModule.cpp"
    "${APPCORE_PRIVATE}/GenericPlatform/GenericApplication.cpp"
    "${APPCORE_PRIVATE}/Windows/WindowsWindow.cpp"
    "${APPCORE_PRIVATE}/Windows/WindowsApplication.cpp"
    "${APPCORE_PRIVATE}/Windows/ConsoleWindow.cpp"
)
target_include_directories(ApplicationCore PUBLIC "${APPCORE_PUBLIC}" "${GENERATED_DIR}/ApplicationCore/Public" PRIVATE "${APPCORE_PRIVATE}")
target_compile_definitions(ApplicationCore PRIVATE APPLICATIONCORE_EXPORTS)
target_link_libraries(ApplicationCore PUBLIC Core)

# -- Engine (SHARED) --
add_library(Engine SHARED
    "${ENGINE_PRIVATE}/Engine/Engine.cpp"
    "${ENGINE_PRIVATE}/Engine/GameEngine.cpp"
    "${ENGINE_PRIVATE}/Engine/EngineDelegates.cpp"
    "${ENGINE_PRIVATE}/GameFramework/GameInstance.cpp"
    "${ENGINE_PRIVATE}/Subsystems/SubsystemCollection.cpp"
    "${ENGINE_PRIVATE}/EngineLoop.cpp"
    "${ENGINE_PRIVATE}/EngineModule.cpp"
    "${ENGINE_PRIVATE}/EngineGlobals.cpp"
)
target_include_directories(Engine PUBLIC
    "${ENGINE_PUBLIC}"
    "${CORE_PUBLIC}"
    "${APPCORE_PUBLIC}"
    "${RENDERCORE_PUBLIC}"
    "${GENERATED_DIR}/Engine/Public"
    "${GENERATED_DIR}/ApplicationCore/Public"
    "${GENERATED_DIR}/RenderCore/Public"
)
target_link_libraries(Engine PUBLIC Core ApplicationCore)
target_compile_definitions(Engine PRIVATE ENGINE_EXPORTS)

# -- Launch (SHARED) -- GuardedMain + GEngineLoop + FLaunchModule
# NOTE: LaunchWindows.cpp (main/WinMain) is NOT included here;
#       entry points belong in the final executable, not the DLL.
add_library(Launch SHARED
    "${LAUNCH_PRIVATE}/Launch.cpp"
    "${LAUNCH_PRIVATE}/LaunchModule.cpp"
)
target_include_directories(Launch PUBLIC "${LAUNCH_PUBLIC}" "${ENGINE_PUBLIC}" "${CORE_PUBLIC}" "${GENERATED_DIR}/Launch/Public")
target_link_libraries(Launch PUBLIC Engine Core)
target_compile_definitions(Launch PRIVATE LAUNCH_EXPORTS)

# -- TestRunner --
add_executable(TestRunner Source/TestRunner/Private/Main.cpp)
target_include_directories(TestRunner PRIVATE
    "${CORE_PUBLIC}" "${ENGINE_PUBLIC}" "${LAUNCH_PUBLIC}"
    "${GENERATED_DIR}/Core/Public" "${GENERATED_DIR}/Engine/Public" "${GENERATED_DIR}/Launch/Public"
)
target_link_libraries(TestRunner PRIVATE Launch Engine Core)

enable_testing()
add_test(NAME TestRunner COMMAND $<TARGET_FILE:TestRunner>)
