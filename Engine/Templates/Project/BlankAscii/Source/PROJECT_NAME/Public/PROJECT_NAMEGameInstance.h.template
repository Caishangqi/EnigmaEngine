// Auto-generated by EnigmaEngine BuildTool. Modify freely.
#pragma once

#include "PROJECT_NAMEAPI.generated.h"
#include "GameFramework/GameInstance.h"
#include <cstdint>

/// PROJECT_NAME game instance.
/// Uses the ASCII renderer for console-based rendering.
/// The engine creates the console window and initializes the renderer
/// automatically from config (DefaultEngine.ini).
class PROJECT_NAME_API FPROJECT_NAMEGameInstance : public Enigma::FGameInstance
{
public:
	void Init() override;
	void Update(float DeltaTime) override;
	void Render() override;
	void Shutdown() override;

private:
	int32_t m_frameCount = 0;
};
